# Copy this file to .env before running `make db-up`.
# .env is gitignored — never commit real secrets.

# Password for the local PostgreSQL container.
# docker-compose.yml reads this automatically.
POSTGRES_PASSWORD=localdev

# Full connection URL — use this in your application code.
DATABASE_URL=postgresql://learnpool:localdev@localhost:5432/learnpool
